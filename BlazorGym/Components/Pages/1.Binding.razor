@page "/binding"

<PageTitle>Counter</PageTitle>

<h1>One-way binding</h1>

<p role="status">Current count: @currentCount</p>

<button class="btn btn-primary" @onclick="IncrementCount">Click me</button>

@code {
    private int currentCount = 0;

    private void IncrementCount()
    {
        currentCount++;
    }
}


@* @page "/binding"

<PageTitle>Counter</PageTitle>

<h1>One-way binding with attribute and lambda</h1>

<p role="status">Current count: @currentCount</p>

<button class="btn btn-primary" disabled="@(currentCount >= 10)" @onclick="() => currentCount++">
    Click me
</button>

@code {
    private int currentCount = 0;
} *@



@* @page "/binding"

<PageTitle>Counter</PageTitle>

<h1>Two-way binding</h1>

<p role="status">Current count: @currentCount</p>

<p><input type="number" @bind-value="@increment" @bind-value:event="oninput" /></p>    

<button class="btn btn-primary" @onclick="IncrementCount">Click me</button>

@code {
    private int currentCount = 0;

    private int increment = 1;      // Ny

    private void IncrementCount()
    {
        currentCount += increment;
    }
} *@


